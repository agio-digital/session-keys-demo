{
  "name": "session-keys-demo",
  "version": "1.0.0",
  "private": true,
  "packageManager": "yarn@4.10.3",
  "workspaces": [
    "app",
    "server"
  ],
  "scripts": {
    "dev": "yarn workspaces foreach -piA run dev",
    "build": "yarn workspaces foreach -A run build",
    "typecheck": "yarn workspaces foreach -A run typecheck",
    "test": "yarn workspaces foreach -A run test:run",
    "test:watch": "yarn workspaces foreach -piA run test",
    "lint": "yarn workspace app lint",
    "format": "yarn workspace app format"
  }
}
